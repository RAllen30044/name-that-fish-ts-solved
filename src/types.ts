// If you have any types that are SHARED BY MANY COMPONENTS,put them here

export type TSFishInfo= {
      fishName:string,
     
      correctCount:number,
      incorrectCount:number,
      correctFishName:string
  }
  
  
export type TSTotalCount={
  totalCount:number
};
